# Backend runtime configuration (example)
# For local dev you usually don't need these, but keep as template for deployment later.

# If you later make CORS configurable in prod:
# CORS_ALLOW_ORIGINS=https://your-frontend.vercel.app

# If you later add DB (optional):
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB

