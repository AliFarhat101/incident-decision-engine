## Models

Training is done in Google Colab (see `ml/notebooks/`).
Exported artifacts are saved here for inference:

- `incident_clf.joblib` (TF-IDF + Logistic Regression pipeline)
- `model_meta.json` (label mapping, thresholds, metrics, training data version)

